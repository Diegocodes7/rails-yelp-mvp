<h1>New Review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: (0..5).to_a, prompt: "Choose a rating" %>
  <%= f.input :content %>
  <%= f.button :submit, "Submit Review", class: "btn btn-success" %>
<% end %>
